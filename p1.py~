import subprocess
command = ('sudo','./interface','-e')
with open('del.js') as input_stream
p = subprocess.Popen(command, stdout=subprocess.PIPE, stderr=subprocess.PIPE, stdin=input_stream)
stdout, stderr = p.communicate()

